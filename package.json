{
  "name": "esim-pro-backend",
  "version": "1.0.0",
  "description": "eSIM Management Platform Backend",
  "scripts": {
    "start": "gunicorn esim_backend.wsgi:application --bind 0.0.0.0:$PORT",
    "build": "pip install -r requirements.txt"
  },
  "engines": {
    "python": "3.11.9"
  },
  "dependencies": {
    "@invertase/react-native-apple-authentication": "^2.4.1",
    "@react-native-google-signin/google-signin": "^15.0.0"
  }
}
